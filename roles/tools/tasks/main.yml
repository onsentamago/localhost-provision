---
# tasks file for tools
- name: add apt ukuu repo
  apt_repository:
    repo: 'ppa:teejee2008/ppa'

- name: install tools
  apt:
    name: "{{ item }}"
    update_cache: yes
  with_items:
  - dconf-editor
  - gnome-tweaks
  - htop
  - ukuu
